@model MrLee.Web.Models.AnularMovimientoVm
@{
    ViewData["Title"] = "Anular movimiento";
}
<div class="row justify-content-center">
    <div class="col-lg-5">
        <div class="section-panel">
            <h5 class="text-danger mb-3">⛔ Anular movimiento laboral</h5>
            <form asp-action="Anular" method="post">
                @Html.AntiForgeryToken()
                <input type="hidden" asp-for="MovimientoId" />
                <input type="hidden" asp-for="EmpleadoId" />
                <div asp-validation-summary="All" class="alert alert-danger mb-3"></div>
                <div class="mb-4">
                    <label class="form-label fw-medium">Motivo de anulación <span class="text-danger">*</span></label>
                    <textarea asp-for="MotivoAnulacion" class="form-control" rows="3" maxlength="400"></textarea>
                    <span asp-validation-for="MotivoAnulacion" class="text-danger small"></span>
                </div>
                <div class="d-flex gap-2 justify-content-end">
                    <a asp-action="Index" asp-route-empleadoId="@Model.EmpleadoId" class="btn btn-outline-secondary">Cancelar</a>
                    <button type="submit" class="btn btn-danger">Confirmar anulación</button>
                </div>
            </form>
        </div>
    </div>
</div>
@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }

}